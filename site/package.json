{
  "name": "asr-companion",
  "private": true,
  "type": "module",
  "description": "Static scientific site for a distribution-free accept/defer gate (Eleventy 3).",
  "scripts": {
    "build": "eleventy --pathprefix=/asr-gate/",
    "serve": "eleventy --serve --pathprefix=/asr-gate/"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2"
  }
}
